# Conclusão: Comparação E01 (make_moons) vs E05 (make_circles)

*(Este arquivo foi criado como template. A conclusão real deve ser preenchida após a execução bem-sucedida das experiências)*

A comparação entre as bases `make_moons` e `make_circles` utilizando a mesma arquitetura de rede neural permite observar o comportamento do modelo diante de diferentes geometrias de dados. Enquanto o dataset `make_moons` apresenta duas intersecções em formato de lua, o dataset `make_circles` possui uma estrutura concêntrica. Ambas as tarefas de classificação binária são não-linearmente separáveis, exigindo a capacidade de generalização da rede configurada com camadas ocultas.
O desempenho em termos de acurácia e loss fornecerá evidências sobre a robustez da topologia escolhida para cada caso de uso.
